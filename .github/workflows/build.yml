name: Build APK

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    container:
      image: kivy/buildozer:latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Build APK
        run: |
          buildozer android debug
